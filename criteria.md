# Критерии оценки модуля «Избранное»

Каждый критерий оценивается по 10-балльной шкале. Максимум: **100 баллов**.

---

## Метрики эффективности генерации

Помимо качества кода, фиксируются показатели эффективности AI-модели:

| Метрика | Описание | Как измерять |
|---------|----------|--------------|
| ⏱️ **Время генерации** | Время от отправки промпта до получения готового результата | Суммарное время всех запросов к модели |
| 💰 **Стоимость генерации** | Затраты на API-вызовы для создания модуля | Сумма input + output токенов × тариф модели |
| 🔄 **Количество итераций** | Число правок до получения рабочего результата | Считать каждый запрос на исправление/доработку |

### Как фиксировать метрики

1. **Время генерации** — засекать от первого промпта до финального ответа с готовым модулем
2. **Стоимость** — рассчитывать по формуле:
   ```
   Cost = (input_tokens × input_price) + (output_tokens × output_price)
   ```
3. **Итерации** — считать:
   - Первоначальная генерация = 1
   - Каждый запрос на исправление ошибок = +1
   - Каждый запрос на добавление функционала = +1

### Идеальные показатели

| Метрика | 🏆 Отлично | 🥇 Хорошо | 🥈 Приемлемо | ⚠️ Много |
|---------|-----------|----------|--------------|---------|
| Время | < 3 мин | 3-5 мин | 5-10 мин | > 10 мин |
| Стоимость | < $0.50 | $0.50-1.00 | $1.00-2.00 | > $2.00 |
| Итерации | 1 | 2 | 3-4 | > 4 |

---

## 1. Базовая функциональность (0-10 баллов)

| Баллы | Требование |
|-------|------------|
| 0-2 | Модуль не устанавливается или падает с ошибками |
| 3-4 | Модуль устанавливается, но основной функционал не работает |
| 5-6 | Базовый функционал работает (добавить/удалить/список) |
| 7-8 | Всё работает, включая миграцию данных гостя |
| 9-10 | Полностью рабочий модуль со всеми заявленными функциями |

**Проверяется:**
- Установка модуля без ошибок
- Добавление/удаление товаров в избранное
- Корректная работа для гостей и авторизованных
- Миграция данных при авторизации

---

## 2. ORM и работа с данными (0-10 баллов)

| Баллы | Требование |
|-------|------------|
| 0-2 | Прямые SQL-запросы или использование старого ядра |
| 3-4 | ORM используется, но с ошибками в структуре |
| 5-6 | Корректный DataManager, но без связей и валидаторов |
| 7-8 | Полноценный DataManager с валидаторами полей |
| 9-10 | Идеальная ORM: связи, валидаторы, кастомные методы выборки |

**Проверяется:**
- Класс `*Table` наследует `Bitrix\Main\ORM\Data\DataManager`
- Корректное описание полей (`getMap()`)
- Использование `IntegerField`, `DatetimeField` и т.д.
- Валидаторы полей (положительный ID, существование пользователя)
- Reference-связи с пользователями/товарами (бонус)

---

## 3. REST Controller D7 (0-10 баллов)

| Баллы | Требование |
|-------|------------|
| 0-2 | AJAX через отдельный PHP-файл без контроллера |
| 3-4 | Контроллер есть, но неправильная структура/namespace |
| 5-6 | Рабочий контроллер с базовыми методами |
| 7-8 | Контроллер с `configureActions()`, правильными правами |
| 9-10 | Идеальный контроллер: фильтры, права, документация методов |

**Проверяется:**
- Наследование от `\Bitrix\Main\Engine\Controller`
- Корректный namespace для автоматического роутинга
- Метод `configureActions()` для настройки доступа
- Методы `*Action()` с правильными сигнатурами
- Возврат данных через `return` (не `echo`)
- CSRF-защита (sessid)

---

## 4. Работа с Cookie D7 (0-10 баллов)

| Баллы | Требование |
|-------|------------|
| 0-2 | Нативные `$_COOKIE` / `setcookie()` |
| 3-4 | `Bitrix\Main\Web\Cookie`, но без шифрования |
| 5-6 | `CryptoCookie` используется, но с ошибками |
| 7-8 | Корректная работа с `CryptoCookie` |
| 9-10 | Идеальная реализация: шифрование, HttpOnly, SameSite, Secure |

**Проверяется:**
- Использование `Bitrix\Main\Web\CryptoCookie`
- Корректные флаги безопасности (`HttpOnly`, `Secure`, `SameSite`)
- Получение cookie через `Context::getCurrent()->getRequest()->getCookie()`
- Установка через `Context::getCurrent()->getResponse()->addCookie()`
- Обработка случая отсутствия `crypto_key` в настройках

---

## 5. События D7 (0-10 баллов)

| Баллы | Требование |
|-------|------------|
| 0-2 | События не используются или в `init.php` |
| 3-4 | События регистрируются, но некорректно |
| 5-6 | Базовая работа с `OnAfterUserAuthorize` |
| 7-8 | Все требуемые события обрабатываются корректно |
| 9-10 | Идеально: регистрация при установке, отписка при удалении |

**Проверяется:**
- Регистрация через `EventManager::getInstance()->registerEventHandler()`
- Регистрация в `install/index.php`, а не в `init.php`
- Корректная отписка при удалении модуля
- Обработка `OnAfterUserAuthorize` для миграции
- Обработка `OnAfterIBlockElementDelete` и `OnAfterIBlockElementUpdate`

---

## 6. Кэширование с тегами (0-10 баллов)

| Баллы | Требование |
|-------|------------|
| 0-2 | Кэширование отсутствует |
| 3-4 | Простое кэширование без тегов |
| 5-6 | Теги используются, но инвалидация некорректна |
| 7-8 | Корректное тегированное кэширование |
| 9-10 | Идеально: теги по товарам, пользователям, грамотная инвалидация |

**Проверяется:**
- Использование `Bitrix\Main\Data\Cache`
- Тегированный кэш через `Bitrix\Main\Data\TaggedCache`
- Регистрация тегов: `registerTag('iblock_id_X')`, кастомные теги
- Инвалидация через `clearByTag()`
- Персональный кэш (по user_id), не глобальный

---

## 7. Работа с инфоблоками D7 (0-10 баллов)

| Баллы | Требование |
|-------|------------|
| 0-2 | Старое ядро: `CIBlockElement::GetList()` |
| 3-4 | D7, но через устаревший `ElementTable` |
| 5-6 | `\Bitrix\Iblock\Elements` с базовым функционалом |
| 7-8 | Корректная работа с API элементов инфоблока |
| 9-10 | Идеально: выборка свойств, фильтрация, кэширование |

**Проверяется:**
- Получение класса через `\Bitrix\Iblock\Iblock::wakeUp($iblockId)->getEntityDataClass()`
- Выборка с `select`, `filter`, `limit`
- Получение свойств товаров
- Проверка существования товара перед добавлением

---

## 8. Архитектура и структура кода (0-10 баллов)

| Баллы | Требование |
|-------|------------|
| 0-2 | Всё в одном файле, нет разделения ответственности |
| 3-4 | Базовое разделение на классы |
| 5-6 | Сервисный слой выделен |
| 7-8 | Чёткое разделение: Controller → Service → Repository |
| 9-10 | Идеальная архитектура: DI, интерфейсы, SOLID |

**Проверяется:**
- Разделение на слои (Controller, Service, Repository, Model)
- Контроллер не содержит бизнес-логику
- Сервис инкапсулирует логику работы с избранным
- Repository изолирует работу с хранилищем
- Отсутствие «God Object» и циклических зависимостей

---

## 9. Безопасность (0-10 баллов)

| Баллы | Требование |
|-------|------------|
| 0-2 | SQL-инъекции, XSS, отсутствие валидации |
| 3-4 | Базовая валидация, но есть уязвимости |
| 5-6 | Валидация параметров, но CSRF не проверяется |
| 7-8 | Полная валидация + CSRF + шифрование cookie |
| 9-10 | Идеально: все проверки, escape, права доступа |

**Проверяется:**
- Валидация `productId` (целое положительное число)
- Проверка существования товара в инфоблоке
- CSRF-защита в API (`check_bitrix_sessid()` или встроенная в Controller)
- Шифрование cookie
- Экранирование вывода (если есть)
- Проверка прав на чтение инфоблока

---

## 10. Публичный компонент (0-10 баллов)

| Баллы | Требование |
|-------|------------|
| 0-2 | Компонент отсутствует или не работает |
| 3-4 | Простая кнопка без стилей и AJAX |
| 5-6 | Рабочий компонент с AJAX, но примитивный дизайн |
| 7-8 | Современный UI, анимации, корректные состояния |
| 9-10 | Идеально: красивый дизайн, плавные анимации, адаптивность, доступность |

**Проверяется:**
- Компонент наследует `CBitrixComponent` и использует D7
- AJAX-взаимодействие через REST Controller модуля
- Визуальные состояния: обычное, hover, активное (в избранном), загрузка
- CSS-анимации (transition/animation для состояний)
- Современный дизайн: иконка сердечка, приятные цвета
- Адаптивность (корректно на мобильных)
- Параметры компонента (`PRODUCT_ID`, `BUTTON_SIZE`, `SHOW_COUNTER`)
- Корректная работа с CSRF-токеном

---

## Бонусные критерии (до +10 баллов)

| Бонус | Баллы |
|-------|-------|
| Модель задаёт уточняющие вопросы перед реализацией | +2 |
| Модель предлагает альтернативные решения | +2 |
| Обработка edge-cases (удалённый товар, максимум в cookie) | +2 |
| Unit-тесты или хотя бы их заготовка | +2 |
| Грамотная работа с транзакциями при миграции | +2 |

---

## Штрафы (до -20 баллов)

| Нарушение | Штраф |
|-----------|-------|
| Использование `init.php` для обработчиков | -5 |
| Прямые SQL-запросы вместо ORM | -5 |
| Использование старого ядра (`CIBlock*`, `CUser`) | -5 |
| Хранение незашифрованных данных в cookie | -3 |
| Отсутствие CSRF-защиты | -2 |

---

## Итоговая формула

```
Итого = Σ(Критерии 1-10) + Бонусы - Штрафы
Базовый максимум: 100 баллов
С бонусами: до 110 баллов
Минимум со штрафами: может уйти в минус
```

## Уровни оценки качества кода

| Баллы | Уровень | Описание |
|-------|---------|----------|
| 90+ | 🏆 Отлично | Enterprise-качество, готово к production |
| 75-89 | 🥇 Хорошо | Качественная реализация с минимальными замечаниями |
| 60-74 | 🥈 Удовлетворительно | Рабочее решение, требует доработки |
| 45-59 | 🥉 Базовый уровень | Базовый функционал, много недочётов |
| <45 | ❌ Недостаточно | Не соответствует требованиям |

---

## Комплексная оценка модели

При итоговом сравнении моделей учитываются все факторы:

### Формула эффективности

```
Эффективность = (Качество кода / 100) × (1 / Нормализованная стоимость) × (1 / Итерации)
```

### Итоговая таблица для сравнения

| Модель | Качество | Время | Стоимость | Итерации | Комментарий |
|--------|:--------:|:-----:|:---------:|:--------:|-------------|
| Model A | XX/100 | X мин | $X.XX | X | ... |
| Model B | XX/100 | X мин | $X.XX | X | ... |

### Рекомендации по выбору модели

- **Для production-задач** → приоритет качеству кода (> 80 баллов)
- **Для прототипов** → приоритет скорости и стоимости
- **Для обучения** → важно количество итераций (модели, требующие меньше правок, лучше понимают контекст Битрикс)

